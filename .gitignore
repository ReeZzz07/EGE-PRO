node_modules/
.next/
dist/
build/
*.log
.env
.env.local

# сырой импорт заданий ФИПИ (JSONL + медиа) — рабочие материалы, не исходный код;
# после прогона scripts/import/solve-tasks.mjs итог уходит в Supabase, а не в git
output/

# отдельное настольное Electron-приложение (решатель заданий) — самостоятельный проект
# со своим node_modules/сборками, живёт вне истории основной платформы
solver-app/

# служебный кэш Supabase CLI (версия бинарника и т.п.) — локальное состояние, не исходный код
supabase/.temp/
