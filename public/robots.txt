# Домен ещё не куплен — ege-pro.ru ниже плейсхолдер, поправить на реальный при деплое
# (см. также VITE_SITE_URL в .env.example и src/lib/seo.ts).
# Ограничивать нечего: у приложения всего 4 настоящих URL (см. src/lib/routes.ts), остальное —
# личный кабинет за авторизацией, доступный только по состоянию React, без отдельного адреса.
User-agent: *
Allow: /

Sitemap: https://ege-pro.ru/sitemap.xml
